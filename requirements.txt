# --- API / servidor ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20    # uploads (UploadFile) — infra de arquivos (#FILES)
pydantic==2.10.4
pydantic-settings==2.7.1
# Exigido pelos modelos OpenAPI do FastAPI (Contact.email = EmailStr)
email-validator==2.2.0

# --- Banco vetorial ---
chromadb==0.6.3

# --- Banco relacional (persistência: usuários, papéis, config, auditoria) ---
SQLAlchemy==2.0.23
# Migrações de schema versionadas do banco real (D-022). Compatível com SQLA 2.0.
alembic==1.13.1
# Cifragem das chaves de provedor em repouso (Fernet)
cryptography==45.0.4
# Conversão PDF→PNG das plantas dos clientes (#MAP — mapa de dispositivos)
pymupdf==1.24.10

# --- Embeddings locais (multilíngue PT/EN) ---
sentence-transformers==3.3.1

# --- Geração com Claude (Anthropic) ---
anthropic==0.42.0

# --- Autenticação (JWT + hash de senha) ---
PyJWT==2.10.1
argon2-cffi==21.3.0

# --- Utilidades ---
python-dotenv==1.0.1

# --- Testes ---
pytest==8.3.4
httpx==0.28.1
